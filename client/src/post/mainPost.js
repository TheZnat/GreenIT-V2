import { PostId } from "./post.js";
import { exitBth } from "./exit";

window.addEventListener("DOMContentLoaded", PostId);
exitBth();

// function onWindowResize() {
//   if (Number(window.innerWidth) === Number(840)) {
//     console.log("Window width is now 840px");
//     // Ваш код здесь, который должен выполняться при ширине окна 800px
//   }
// }

// // Добавляем обработчик события resize
// window.addEventListener("resize", onWindowResize);

// // Проверяем размер окна при первой загрузке страницы
// window.addEventListener("load", onWindowResize);
